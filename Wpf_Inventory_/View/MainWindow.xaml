<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_Inventory_"
        xmlns:Classes="clr-namespace:Wpf_Inventory_.Classes" xmlns:Controls="clr-namespace:Wpf_Inventory_.View.Controls" x:Class="Wpf_Inventory_.MainWindow"
        mc:Ignorable="d"
        Title="База данных технических устройств" Loaded="Window_Loaded"
        Width="900" Height="521">

    <Window.Resources>
        <Classes:NullableBoolToBoolConverter x:Key="NullableBoolConverter"/>
    </Window.Resources>

    <DockPanel>
        <!-- Нижняя строка состояния -->
        <StatusBar DockPanel.Dock="Bottom" Background="#2D2D30" Foreground="White">
            <StatusBarItem>
                <TextBlock x:Name="StatusIndicatorText" Text="Готово" Margin="10,0"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <Ellipse x:Name="StatusIndicator" Width="10" Height="10" Fill="Green" Margin="10">
                    <Ellipse.ToolTip>
                        <ToolTip>
                            <StackPanel>
                                <TextBlock Text="Значение индикатора:" FontWeight="Bold"/>
                                <TextBlock Text="Зелёный — Подключение установлено (онлайн режим)" Foreground="Green"/>
                                <TextBlock Text="Красный — Нет подключения или нет данных" Foreground="Red"/>
                                <TextBlock Text="Оранженвый — Выполняется фоновый процесс" Foreground="Orange"/>
                                <TextBlock Text="Фиолетовый — Оффлайн режим" Foreground="Purple"/>
                            </StackPanel>
                        </ToolTip>
                    </Ellipse.ToolTip>
                </Ellipse>
            </StatusBarItem>
        </StatusBar>
        <Menu DockPanel.Dock="Top" Background="#2D2D30" Foreground="White">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel LastChildFill="False"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
           

            <!-- Левые элементы меню -->
            <MenuItem Header="Файл" DockPanel.Dock="Left">
                <MenuItem Foreground="Black" Header="Импорт" Click="DevicImportButton_Click"/>
                <MenuItem Foreground="Black" Header="Экспорт" Click="DeviceExportButton_Click"/>
                <MenuItem Foreground="Black" Header="Пинг Сервера" Click="PingServerButton_Click"/>
                <MenuItem Foreground="Black" Header="Пинг Базы данных" Click="PingDBButton_Click"/>
                <MenuItem Foreground="Black" Header="Сохранить" Click="DeviceSaveButton_Click"/>
                <MenuItem Foreground="Black" Header="Выход" Click="Exit_Click"/>

            </MenuItem>
            <MenuItem Header="Редактировать" DockPanel.Dock="Left">
                <MenuItem Foreground="Black" Header="Копировать"/>
                <MenuItem Foreground="Black" Header="Вставить"/>
            </MenuItem>
            <MenuItem Header="Данные" DockPanel.Dock="Left">
                <MenuItem x:Name="ToggleModeMenuItem" Foreground="Black" Header="Загрузка..." Click="ToggleMode_Click"/>
                <MenuItem Foreground="Black" Header="Создать пустой кэш" Click="CreateEmpytyCache_Click"/>
                <MenuItem Foreground="Black" Header="Синхронизировать" Click="DeviceSyncButton_Click"/>
                <MenuItem Foreground="Black" Header="Заполнить таблицу с сервера" Click="LoadFromServer_Click"/>
                <MenuItem Foreground="Black" Header="Заполнить таблицу с кэша" Click="LoadFromCache_Click"/>
            </MenuItem>
            <MenuItem Header="Помощь" DockPanel.Dock="Left">
                <MenuItem Foreground="Black" Header="О программе" Click="AboutButton_Click"/>
            </MenuItem>

            <!-- Индикатор активности (в правом верхнем углу) -->
            <Grid HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,5,10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="16*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="0*"/>
                </Grid.ColumnDefinitions>
                <Grid.ToolTip>
                    <TextBlock x:Name="StatusTooltip" Text="Система работает стабильно"/>
                </Grid.ToolTip>
            </Grid>

        </Menu>

        <!-- Основное содержимое -->
        <TabControl DockPanel.Dock="Top">

            <TabItem Header="Устройства">
                <Controls:DeviceControl/>
            </TabItem>

            <!--<TabItem Header="Рабочие места">
                <Controls:WorkspaceControl Height="401" Margin="-4,0,15,0"/>
            </TabItem>-->

            <TabItem Header="Офисы">
                <Controls:OfficeControl/>
            </TabItem>

            <TabItem Header="Отделы">
                <!--<Controls:DepartmentControl/>-->
            </TabItem>

            <TabItem Header="Типы">
                <!--<Controls:DeviceTypeControl/>-->
            </TabItem>

            <TabItem Header="Все данные">
                <!--<Controls:DeviceControl/>-->
            </TabItem>

        </TabControl>

        <!-- Нижняя строка состояния -->
    </DockPanel>
</Window>
